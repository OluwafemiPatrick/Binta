import 'package:flutter/material.dart';

class LiveSupport extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Live Support'),
    );
  }
}
